apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ades-config-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: glusterfs-storage
  resources:
    requests:
      storage: 100Mi